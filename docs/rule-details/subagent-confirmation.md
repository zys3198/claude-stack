# 子代理调用确认细则

来源：全局 `CLAUDE.md` §2.5（2026-09-19 结构搬移，内容未改）。

- 无论是否触发具体 skill，准备调用子代理前，先向用户展示拟选模型（可见时写实际名称）、provider、route、effort、并发数和隔离方式。
- 同时判断配置是否合理，说明原因、影响和备选方案；询问用户是否采用，未获确认前不启动子代理。
- 不绑定固定模型或供应商；宿主未暴露的配置写 `unverified`。用户已确认同一配置时不重复询问。
