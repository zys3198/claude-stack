## 判级：已知的已知

- **目标明确**：`user` 表新增 `age` 字段。
- **范围明确**：更新 DDL 和 MyBatis 映射。
- **验收明确**：字段能够正常读写。
- `age` 已被明确命名，不需要改问出生日期等替代方案。
- 数据库类型、DDL 文件位置、MyBatis 使用实体/XML/注解等属于应通过源码确认的实现细节，不因此升级级别。

## 按级出牌：直接执行，但当前被环境阻塞

当前目录 `C:\Users\zys31\AppData\Local\Temp\skill-up-649142546` 只包含 `.claude/skills/expose-unknowns`，不是项目目录；上下文检查结果也确认：

- 无 Git 仓库
- 无项目指令
- 无 DDL、Java 实体或 MyBatis 映射文件

因此我没有修改代码，也无法验证读写链路。

## 实际改动前的确认边界

拿到正确项目目录后，我会先定位并核对：

1. `user` 表对应的 DDL/迁移文件；
2. `User` 实体及 MyBatis `resultMap`、查询、插入、更新映射；
3. 现有测试或新增最小读写测试。

由于这是数据库结构变更，且预计至少涉及 DDL、实体/映射和测试等多个文件，确认门禁会在确定实际文件清单后触发；不会擅自决定 `NULL`、默认值或回填策略。

请将工作目录切换到实际项目，或提供项目路径；之后我再继续勘察并给出待改文件清单。
