判级：**已知的已知**。字段名、改动范围和验收标准都明确，不需要 `grill-me` 采访或方案选型。

按级出牌：

- 分类：开发新功能（数据库 Schema 变更）
- 主路径：直接定位 `user` 表的 DDL、实体/ResultMap、SQL 映射和现有测试
- 必要闸门：Schema 变更确认、读写测试；修改后做独立复核
- 当前状态：**阻塞，未改代码**
- 原因：当前目录 `C:\Users\zys31\AppData\Local\Temp\skill-up-3469396301` 不是项目仓库，只包含 `expose-unknowns` 技能文件，找不到 DDL 或 MyBatis 映射
- 下一步：请在目标项目目录重新打开会话，或提供目标项目路径；我会先盘点实际文件和现有数据库方言，再继续修改

目前没有产生任何项目文件改动。
