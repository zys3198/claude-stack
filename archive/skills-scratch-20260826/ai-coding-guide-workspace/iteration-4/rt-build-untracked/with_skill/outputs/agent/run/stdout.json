域归属： 编码（构建失败排查）/ ai-coding-guide 当前会话可用

| 项 | 路由契约 |
|---|---|
| **分类** | 构建错误 |
| **主路径** | 跑项目已有构建命令，按错误原文定位根因（必要时用 `inspect_context.py` 拿 manifests 证据） |
| **组合** | git 状态检查 + 未跟踪文件范围确认；条件路径：对应语言/框架专项工具（当前会话可见时启用） |
| **闸门** | 修复后必须 build pass；删除/清理未跟踪文件前展示清单并用户确认 |
| **下一步** | 先展示 git status 与最近改动，收齐完整构建错误和构建命令，再定位根因并修复 |
