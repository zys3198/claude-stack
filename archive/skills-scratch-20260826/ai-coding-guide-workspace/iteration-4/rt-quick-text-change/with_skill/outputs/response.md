按 ai-coding-guide 分诊结果：

**分类**：快速改动（单文案替换，命中 Step 1「快速改动」）  
**主路径**：直接最小改动 → 定位按钮代码后执行一次文本替换（`Read/ctx_read` → `Edit/ctx_patch`）  
**组合**：无  
**闸门**：最小检查（改后确认只影响目标按钮文案，不强制 review / verify）  
**下一步**：直接执行，如用户确认即可查找并替换『提交』为『保存』
