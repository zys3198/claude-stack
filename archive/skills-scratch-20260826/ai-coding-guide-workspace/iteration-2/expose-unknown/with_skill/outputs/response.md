**触发门禁**
- 域归属：编码 —— 开工路由判断，属本路由范围
- ai-coding-guide 当前会话？YES（已加载）
- 域归属为编码 → 进 Step 1

**Step 1 信号命中**：「这个需求我没思路」+「开工前先把盲点摊出来」→ **判级/暴露未知**（低频分类，已读 references 细节）。

**环境自检**：`expose-unknowns`、`superpowers:brainstorming`、`code-change-workflow` 当前会话均可调用。

---

## 路由契约

**分类**：判级/暴露未知

**主路径**：`expose-unknowns` —— 判四象限（已知已知 / 已知未知 / 未知已知 / 未知未知）→ 按级选技巧 → 任务后反考

**组合**：`superpowers:brainstorming`（条件路径，当前会话可调用）—— 判到「未知的未知」需采访澄清时内嵌，不重复展开；fallback 为 `code-change-workflow` §1.1 判级一行

**闸门**：判级产物不产代码，不套 TDD/review/verify；收尾用 expose-unknowns 自带**反考复盘**验证盲点是否真被摊开。真正开工进入实现后，再按实际风险（Step 0.6）重定闸门

**下一步**：按你的要求只停在路由层，不执行。确认后我启动 `expose-unknowns`，先采访你把盲点摊开；若当时说不出关键背景（目标/约束/验收），采访由 AI 递参考问题兜底，不死等

---

关键一句：不走「开发新功能」的澄清流程，因为你现在的问题不是"做什么"，而是"不知道自己不知道什么"——先判级再谈实现，顺序不对会带着盲点开工。
