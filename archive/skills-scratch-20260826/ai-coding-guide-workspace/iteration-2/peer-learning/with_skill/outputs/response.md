路由判断如下（只路由，不动手）。

```
分类：学习型开发 —— 你要练 debug 判断力且明确「别直接修」，目标在练判断不在修 bug，
      故不走「调试 bug」分类（那是实际修 bug 的流程）
主路径：ai-coding-coach（当前会话已装，直达）—— 对练模式：你先说假设 → 我纠偏，不碰代码
组合：无（纯陪跑，不产代码改动）；纠偏时以 code-change-workflow §2 调试工作流
      / systematic-debugging（条件）作对照标杆，不自己上手修
闸门：无代码闸门（不产码）；每次纠偏按「结论 + 依据」给对照，不只说对错，防感觉式判断
下一步：等你给第一个 debug 场景 + 假设 —— 你出题、先说判断，我按证据纠偏
```

下一步就是你的了：抛一个具体 bug（现象 + 你的假设）过来即可。
