按 ai-coding-guide 开工路由，先跑触发门禁，再给五项路由契约（只路由，不执行）。

**触发门禁**
```
域归属： 编码域（路由指南维护）—— 任务是审查/优化 learning-guide 这个 skill 文件本身，不是用它在学习域做学习任务
ai-coding-guide 在当前会话？ YES
若域归属非编码 → 转介：不适用
```

**开工问询**（速判）：需求明确（审查+优化一个指定 skill）、有既定分类，跳过逐问；归属默认**我动手**——审查+优化是执行型，非学习陪跑。

---

**分类**：路由指南维护（Step 1 信号表「审查/优化 guide skill」精确命中；不因对象是 learning-guide 而误判为学习型开发——目标不是学东西，是改 skill 文件）

**主路径**：`guide-skill-auditor`（其定位正是「guide 体检 / 误路由排查 / 审查路由 skill」，与本请求完全对口；learning-guide 属路由类 skill，交由它做审计先行）

**组合**：
- 若审查发现学习/教程域判定逻辑有行为变化 → 叠加 `darwin-skill` 判演化方向
- 与 ai-coding-guide 的域边界存在交叉处 → 对照本指南 references 复核，防互斥/死引用
- 小修则直接最小改动，不再叠加其他能力

**闸门**：
- 审计先行：先出审计结论（问题清单 + 证据），改动前确认范围——skill 文件改动触及多文件时触发 §1.3 人工确认线
- 兜底：只评估时给结论不改文件
- 收尾：无代码产物，不强制 verify；改完用 audit 复核一致性

**下一步**：等你点头后进入 `guide-skill-auditor` 执行——先出审计结论，再谈优化；本轮只给了路由，未动任何文件。

一句话解释为何不是另一路径：这不是转介给 learning-guide 的学习任务（域边界那条只管「学习/做教程」场景，不覆盖「审查这个 skill 本身」），所以留在编码域走审计路由。
