开工路由判断完成（只做路由，不执行任务）。先给门禁，再给五项契约。

---

**门禁**

```
域归属： 编码（前端视觉）—— 请求含「落地页」且要视觉产出，属前端视觉子路径
ai-coding-guide 在当前会话？ YES（本次已调用）
若域归属非编码 → 不适用，无需转介
```

---

**五项路由契约**

**分类：** 前端视觉 · 方向未定（新页面、无设计稿、无参考/品牌约束，"现代好看"未定义具体方向）

**主路径：** 先做决策点问询 → 给 2-3 个视觉方向选项让你选；选定后走 `hallmark`（新页面，反 AI 大路货）。默认路径明确或你说"直接做/你定"则跳过问询，直接走 `hallmark`。

**组合：** 定方向 → `hallmark` 出视觉方向 → 按项目栈实现（先查现有依赖复用，不新引入组件库）→ 验证串联 `browser-testing-with-devtools`（本机 chrome-devtools 已配置）。可选项：`taste-skill:*` / `apple-design` 作风格叠加（点名才套，不默认堆叠）。串行不并联。

**闸门：** 方向确认（方向未定必须你点头，不让设计 skill 拍脑袋）→ 完成后浏览器运行验证（行为 + 视觉证据），不假完成。

**下一步：** 问 1 个关键问题——你想要哪个方向的落地页？（例如：A. 深色科技感 / B. 浅色简约大气 / C. 渐变品牌色 + 插画，或你给参考链接/品牌约束）

---

一句裁决理由：这不是"纯前端工程"，因为"好看一点"是明确的视觉方向诉求，且混合意图「做页面 + 写代码」按前端视觉子路径设计先行，而非直接进编码通用分支。
