**分类**：交付型小改动 / skill 自身配置补词 / `small` 档

**主路径**：精简单文件编辑路径，不走 `REQUIREMENT→DESIGN→...` 状态机；命中「最小改动」且仅补一个词，适合 caveman/direct-edit。

**组合**：单文件单点修改，无需子 Agent，无需多阶段产物。

**闸门**：`BLOCKED`——关键输入缺失：
1. 「这篇文章」未提供（无文章路径或内容）；
2. 待补的「一个触发词」未指明；
3. ai-coding-guide 中具体补到哪个位置/哪个分诊表未明确。

**下一步**：请用户提供文章路径/内容、目标触发词，以及希望补进 ai-coding-guide 的哪个分诊表或文件位置；确认后执行单文件最小改动。
