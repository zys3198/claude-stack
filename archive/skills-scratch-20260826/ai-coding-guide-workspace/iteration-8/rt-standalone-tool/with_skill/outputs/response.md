域归属： 非编码（多媒体格式转换，不属于开发/前端/构建/调试/审查/提交任一编码分类）  
ai-coding-guide 在当前会话？ YES  
若域归属非编码 → 作为独立工具请求直接执行，不进 ai-coding-guide 状态机  

---

**路由契约（5 项）：**

- **分类**：独立工具请求 / 非编码交付  
- **主路径**：直接执行（ffmpeg CLI：`ffmpeg -i <输入视频> <输出>.mp3`），不进入 `ai-coding-guide` 交付状态机  
- **组合**：无  
- **闸门**：文件覆盖/输出路径确认（写文件前确认目标路径，避免误覆盖）  
- **下一步**：请提供视频文件路径，确认后执行转换命令
