域归属：编码（文案级改动，非写作/学习域）
ai-coding-guide 在当前会话：YES

五项路由契约：

| 项 | 判定 |
|---|---|
| **分类** | 快速改动 |
| **主路径** | 直接最小改动（定位按钮文案所在文件 → 单文件替换） |
| **组合** | 无 |
| **闸门** | 最小检查：确认替换的是目标按钮、无同名文案误伤 |
| **下一步** | 直接执行 |

说明：用户已明确拒绝复杂流程，故不走交付状态机，也不弹 A/B/C；按 `快速改动` 默认路径立即改即可。
