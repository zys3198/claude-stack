域归属：编码域工具使用（循环任务）  
ai-coding-guide 在当前会话？ YES

按 `routing.md` Step 1 信号「每 5 分钟检查」命中 **循环任务** 分类。

路由契约（按你要求的五项输出）：

- **分类**：循环任务 / 状态轮询
- **主路径**：`/loop 5m <检查命令>`（当前会话可见 `loop` skill）
- **组合**：无（若任务来自外部系统，可组合 `agent-reach` / `TaskGet` / 对应 MCP；当前未指定任务来源）
- **闸门**：无代码产物；启动前必须确认「检查对象 + 停止条件 + 通知方式」，避免无限空转
- **下一步**：问 1 个关键问题——你要检查的是哪个任务（任务 ID / 进程 / 分支 / URL）？以及检查到什么状态时停止或通知你？
