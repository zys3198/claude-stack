按 `ai-coding-guide` 的分诊框架，你的请求属于**路由指南维护（元设计咨询）**，只做路由判断、不修改文件。

| 项 | 契约 |
|---|---|
| **分类** | 路由指南维护 —— 在问 `ai-coding-guide` 自身的四职责边界，不是编码交付任务。 |
| **主路径** | `guide-skill-auditor`（评估模式：给结论、不改文件）。 |
| **组合** | 无附加执行 skill；四职责的事实源分散在 `routing.md:7-13`、`ecosystems.md`、`SKILL.md`「不可绕过」、「ai-coding-coach`。 |
| **闸门** | 证据优先——只引用 `routing.md` 已写明的职责定义，不擅自扩展；若要改 `description` 或 `routing.md` 必须展示 diff 并确认。 |
| **下一步** | 给出定位结论：`ai-coding-guide` 是**编码域总入口**，四职责分层为——开工路由（唯一主目标）、生态地图（索引层）、质量闸门（横切规则）、学习陪跑（转介 `ai-coding-coach` 的入口）；下游执行权不归它，分别交给对应专项 skill / agent / 交付状态机。 |
