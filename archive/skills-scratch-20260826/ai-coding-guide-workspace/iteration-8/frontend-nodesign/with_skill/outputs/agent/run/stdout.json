**触发门禁**  
- 域归属：编码（含前端视觉）——请求含「登录页」且要「视觉方向」产物，命中编码域前端视觉子路径。  
- ai-coding-guide 在当前会话：YES（已调用）。  
- 非编码：否，继续本路由。  

---

**路由契约（五项）**

| 项 | 结论 |
|---|---|
| **分类** | 前端视觉 —— 方向未定（新页面 / 无设计稿 / 要视觉方向） |
| **主路径** | 先 **AskUserQuestion 给 2-3 个视觉方向选项** → 用户选定后调用 `hallmark`（新页面/反 AI 大路货设计） |
| **组合** | 方向确定后进入实现阶段：按项目现有栈复用依赖（必要时跑 `inspect_context.py` 定栈）；验证可串联 `browser-testing-with-devtools` / `playwright` |
| **闸门** | 方向选择阶段无硬闸门；实现后必须走 verify（build/lint + 浏览器/截图验证），不直接宣布完成 |
| **下一步** | **问 1 个关键问题**：给出 2-3 个视觉方向选项，等用户选定后再进入 `hallmark` |
