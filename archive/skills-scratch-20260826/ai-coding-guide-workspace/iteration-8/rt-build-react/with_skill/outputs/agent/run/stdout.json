域归属：编码（含前端视觉）——"React 项目 build 报错"属于工程构建问题，不归写作/学习。  
ai-coding-guide 在当前会话？ YES  
进 Step 1。

路由契约：

**分类**：构建错误（routing.md Step 1 信号表："build 报错""类型错误"优先于调试 bug）  
**主路径**：直接排查项目已有构建命令，按错误原文定位根因；先用 `python3 <SKILL_ROOT>/scripts/inspect_context.py .` 拿项目证据，免问框架。  
**组合**：必要时复跑 `npm run build` / `pnpm build` 等捕获完整错误栈；React 相关条件路径见当前会话可用专项（无则直接用命令排查）。  
**闸门**：构建错误修复前必须看到「完整错误原文 + 执行命令 + 最近改动」；修复后必须跑过同一构建命令并确认通过，才能宣布完成。  
**下一步**：需要你提供 3 样信息：① 构建命令（如 `npm run build`），② 完整报错输出（文本或截图），③ 最近是否改过依赖/配置/源码。拿到后即可开始排查。
