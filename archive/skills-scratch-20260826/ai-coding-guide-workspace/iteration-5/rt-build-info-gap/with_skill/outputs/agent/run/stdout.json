域归属：编码 / 构建错误 —— 用户明说 "build 报错了"。  
ai-coding-guide 在当前会话？YES

- **分类**：构建错误
- **主路径**：运行项目已有构建命令并按错误原文排查；先跑 `python3 <SKILL_ROOT>/scripts/inspect_context.py .` 拿技术栈/构建脚本证据，免问框架
- **组合**：无
- **闸门**：横切收尾 —— 修复后必须重跑同一构建命令，确认错误消失；涉及删除/清理未跟踪文件需单独确认
- **下一步**：需要你提供以下缺口信息（按优先级）：
  1. 你执行的构建命令（如 `npm run build`、`cargo build`、`pnpm build` 等）
  2. 完整错误日志 / 终端输出
  3. 最近改动（改了哪些文件、是否新增依赖、是否切换分支）
  4. 期望结果（例如应该构建成功、还是只想消除某条特定报错）

如果错误日志很长，可直接粘贴最后 50 行；我先定位根因，不盲试。
