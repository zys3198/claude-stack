**触发门禁**（§0）
```
article-writing-guide 相关？ NO —— 视频转 MP3 是媒体格式转换，属执行型工具任务，不在写作生命周期任何阶段
目标 skill 在当前会话？ 不适用（无写作 skill 命中）
若 NO → 最接近替代：无写作替代；直接走工具执行（ffmpeg）
```

**五项路由契约**

**分类**：执行型任务（文件格式转换）。不属于写作域、学习域或调研域任一——本路由的"不用于"判定命中，写作 skill 全部不委派。

**主路径**：不走本路由表。主路径 = 直接工具执行：检查 ffmpeg 可用性 → 转码命令 → 验证输出。不调用任何 article-writing-guide 下游 skill。

**组合**：无组合。单点操作，不跨阶段，不触发 🔴 CHECKPOINT，无 pipeline。

**闸门**：写作闸门（依据表/basis map）不适用。执行前需人工确认 3 项：
- 视频文件路径及存在性
- 输出文件名/目录
- ffmpeg 是否已安装（未装则先装）

单文件转换不触发 §1.3 的 ≥3 文件确认线，但路径/输出位置确认不可省。

**下一步**：请提供视频文件路径（含期望输出目录，可选）。拿到后我先验证文件存在与格式，再执行 ffmpeg 转换并核对输出的 MP3。

只做了路由判断，未执行转换。要开始的话给我文件路径即可。
