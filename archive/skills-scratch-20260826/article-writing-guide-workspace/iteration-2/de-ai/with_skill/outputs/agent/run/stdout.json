**分类**：⑤ 去 AI 味 / 润色（改写·文风层），非结构重构，非排版。

**主路径**：`human-writing`（改写模式）——2026-08-08 拍板完全替代 `ai-text-polisher`，改写四选中的文风向唯一入口。

**组合**：
- 纯文风问题 → 单点 `human-writing`，无其他 skill 叠加。
- AI 味根因治理分支：若 AI 味源于"一次性生成没搭骨架"（结构臃肿、逐段无重心）而非纯措辞问题 → 先走 `article-writer` §7.10 搭骨架五步法（冻结细骨架再逐节扩），再回 `human-writing` 处理表层文风。**这条需在执行时判根因，路由阶段不预判。**
- 可选串行（视需求）：`edit-article`(结构) → `human-writing`(文风) → `chinese-markdown-normalizer`(排版)。
- 若最终发布 → 末环 `publish-final-check`；本次请求未提发布，不进 pipeline。

**闸门**：
- 写入闸门：改稿先在对话预览，用户确认后才落盘文件。
- 决策点先问：文体 / 读者 / 目的三要素不全则先问清（无明确标杆时）。
- 依据表 + basis map：改写产出正文时随预览交付，basis 以现稿原文为主；新增句子禁止无依据。
- 特征来源门：先读 2-3 篇同作者/目标风格的低 AI 率样本提炼正面特征，**对照改而非凭空套规则**（`human-writing` 强制）。

**下一步**：开工问询（只做路由，不执行）——只需用户先给两项输入：① 文章文本或路径；② 有没有目标风格标杆/样本。拿到后即可进入 `human-writing` 改写模式，先出「AI 味诊断 + 改稿预览」，确认后再动文件。

路由结论：单点改写，命中 `human-writing`，无跨阶段 CHECKPOINT；是否叠加骨架重建取决于执行时的根因判断。
