---
name: plagiarism-audit
description: 对照外部来源审查文章的抄袭/高仿/洗稿。派多个评判员对抗式逐句比对，区分"技术事实描述（允许）"和"原创措辞搬运（必改）"，多轮复判直到 PASS。Use when 用户提供外部来源（URL/文件路径/粘贴文本）并要求查文章是否抄了这些来源，或用户怀疑文章直接贴了原文、洗稿、与原文高度相似。也适用于"审查是否遵守'只可参考不可抄袭'要求"。
---

# Plagiarism Audit

对照一个或多个外部来源，审查文章的抄袭/高仿/洗稿。核心难点不是"找相同词"，而是区分**技术事实描述（允许）**和**原创措辞搬运（必改）**。

## 触发

- 用户给文章 + 外部来源（URL/文件/粘贴），要查"是不是抄了"
- 用户说"怀疑直接贴原文""洗稿""高仿""与原文高度相似"
- 用户要求审查"只可参考不可抄袭"是否被遵守
- multi-review-pipeline 检测到用户提供来源时，自动作为第 5 个 reviewer 调用本 skill

## 来源边界（先问清/确认）

每条来源标注许可级别：
- **可引用原文**：目标项目自有文档、官方文档/源码、技术规范
- **仅可参考**：第三方文章、博客、教程、他仓文档——严禁逐字/高仿/同义改写搬运

未标注时主动问用户。默认第三方来源一律按"仅可参考"。

## 工作流

### Step 1: 收集来源（失败必须多路重试）

每条 URL 抓取失败时，按顺序重试，全失败才能报"无法获取"：
1. 原始 URL（webReader / WebFetch）
2. `raw.githubusercontent.com` + 中文路径 URL 编码（GitHub 来源）
3. `gh api repos/<owner>/<repo>/contents/<path>` 拿 download_url 再 curl，或加 `-H "Accept: application/vnd.github.raw"` 直取正文
4. WebSearch 找缓存/转载

**禁止**：单次抓取失败就标"无法获取"跳过该来源——这是最大的漏审来源。

来源到手后，摘出每条的**原创元素清单**：原创举例组合、原创比喻、原创金句、原创论述框架、原创句式封装。

### Step 2: 派发对抗式评判员

并行派 2-3 个 `general-purpose` agent，每个拿到：文章路径 + 某来源的原创元素清单 + 判定标准。

判定标准（评判员 prompt 必含）：
- **允许（不算抄）**：官方原话、源码常量/函数名/提示词原文、技术名词、固定结构模板、技术事实
- **必改**：原创举例组合、原创比喻、原创金句句式、原创论述框架、逐句对齐只换近义词、同义改写

输出格式：
```
行号: [文章句] ≈ [来源句] | 判定: 逐字抄袭/高仿/允许 | 理由
```

评判员标准会漂移，单轮 PASS 不可信。

### Step 3: 改正（同主题一次性全改）

**关键：姐妹段检查。** 同一例子/论点常分散在多处（正文段 + 代码块 + 表格），改一处忘改另一处是高频漏。改前先按主题 grep 出全部位置，一次全改。

改写策略（脱钩顺序）：
1. 换切入角度（如从"该存什么"换成"维护成本"）
2. 换自己的举例（换成目标读者场景，如 Java 换 Go）
3. 换叙述顺序、换句式封装
4. 技术事实保留，措辞重写

### Step 4: 复判至 PASS

改完再派评判员（最好换不同 agent 视角）。每轮常会新发现漏网。循环直到 PASS。

评判员把**技术事实描述**当抄袭卡时（如 JSONL 的"追加/清空"是机制本身），人工兜底：判定为客观机制描述则保留，仅措辞搬运才改。

## 不要做什么（反例）

| # | 反模式 | 正确做法 |
|---|--------|---------|
| 1 | 单次抓取失败标"无法获取"跳过来源 | 多路重试（raw/编码/gh api/搜索），全失败才报 |
| 2 | 改一处忘改同主题姐妹段 | 改前 grep 全部位置一次全改 |
| 3 | 单轮评判员 PASS 就收工 | 至少两轮独立复判，评判员会漂移 |
| 4 | 把技术事实描述当抄袭改 | 区分客观机制（允许）vs 措辞搬运（必改） |
| 5 | 只查逐字相同 | 同义改写、句式重合、原创比喻/金句借用都算 |
| 6 | 不区分来源许可级别 | 官方/自有可引用，第三方仅参考，未标注先问 |

## 详细参考

抓取重试命令模板、评判员 prompt 模板、原创元素提取清单：见 [REFERENCE.md](REFERENCE.md)
