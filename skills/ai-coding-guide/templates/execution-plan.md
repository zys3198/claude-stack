<!-- devflow-template: execution-plan@1.1 -->
# 执行计划

> 保持计划紧凑。只记录执行所需的目标、任务、验证和风险；实际变化与偏差写入 change report，不在这里重复维护过程日志。

## 目标与完成定义

{{说明用户最终获得什么能力，以及怎样观察到它已经工作}}

## 上下文与范围

{{说明当前实现、关键模块、仓库相对路径、约束和非目标}}

## 执行任务

<!-- 必须使用下面的 Markdown 任务块；不要改成 YAML/JSON、parallel_tasks 或自由表格。 -->

### 任务 T1：{{动作导向的任务名}}

- 状态：`pending`
- 依赖：{{无，或任务 ID}}
- 目标文件：`{{仓库相对路径}}`
- 具体动作：{{说明修改位置、行为和必要接口}}
- 完成证据：{{说明本任务完成后可观察的结果}}

<!-- 按需复制完整任务块。标题必须以“### 任务”开头；每块必须保留“状态”和“目标文件”。 -->

## 验证与验收

现有测试能力：{{列出仓库已经存在的测试层级、配置/目录和命令；特别标明是否已有 integration、contract 或 E2E}}

覆盖映射：{{把验收标准和高风险行为映射到项目已有的合适测试层级；已有 E2E 且本次涉及其用户旅程时必须安排补充或更新}}

在 `{{工作目录}}` 运行：

```bash
{{项目原生验证命令}}
```

<!-- bash 代码块允许使用 # 注释；至少包含一条实际可执行命令。 -->

预期观察：{{具体输出、行为、响应或通过条件}}

## 风险、恢复与回滚

{{说明高风险步骤、可重试性、失败恢复和回滚方法}}
