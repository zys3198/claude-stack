---
name: article-writing-guide
description: 文章写作总路由器——按写作阶段/场景把请求分发到具体的写作 skill（调研→起草→改写→配图→审校→查重→发布前总检）。覆盖通用技术写作与 JavaGuide 专属模式。Use when 用户要写文章/改文章/审文章/查重/配图但不知道用哪个 skill，或一句话说不清该走哪条路；也触发于"写一篇""帮我写""这篇文章怎么改""发布前检查""JavaGuide 文章"等泛化写作请求。先读本 skill 定路由，再调用具体 skill。
---

# Article Writing Guide（写作总路由）

## 0. 何时用本 skill

用户提写作需求但**没点名具体 skill**，或点名的不一定是最优解时，先读本 skill 定路由。已明确点名（如"用 ai-text-polisher 润色"）则直接跳过本 skill。

**废弃 skill 迁移**：用户点名 `javaguide-writer`（已合并）→ 改用 `article-writer` 指定 JavaGuide 模式。

**语言边界**：本路由默认**中文**写作。英文文章可走 `article-writer` / `edit-article`，但跳过中文专用 skill（`chinese-markdown-normalizer`、`ai-text-polisher`）。

## 1. 一句话路由表

按 **写作生命周期阶段** 分发：

| 阶段 | 用户在说什么 | 调用 skill |
|------|-------------|-----------|
| ① 选型/调研 | "这个主题怎么写""有没有相关资料""帮我研究下 X" | `doc-finder`(仓库内) / `deep-research`(全网) / `hv-analysis`(深度研究报告) |
| ② 找证据（问答场景，非写作链路） | "从这几篇文章里提取回答 X 的依据" | `answer-evidence-finder` |
| ③ 起草-从零 | "写一篇关于 X 的文章""帮我写篇博客/教程/方案" | `article-writer`（默认通用模式） |
| ③ 起草-JavaGuide | "写 JavaGuide 文章""按 docs/ai 规范写" | `article-writer` 指定 **JavaGuide 模式** |
| ③ 起草-素材成型 | "我有一堆笔记/碎片，帮我拼成文章" | `writing-fragments`(先挖) → `writing-shape`(再塑形) |
| ③ 起草-叙事节拍 | "按故事线/节拍一步步写" | `writing-beats` |
| ④ 结构改写 | "重新组织这篇""调整章节结构""逻辑不顺" | `edit-article` |
| ⑤ 去 AI 味/润色 | "AI 味太重""改得像人写的""优化文风" | `ai-text-polisher` |
| ⑥ 排版规范化 | "统一 Markdown 格式""修中英文空格/标点""批量规范" | `chinese-markdown-normalizer` |
| ⑦ 配图 | "要不要加图""画个流程图/架构图""配图" | `drawio-article-illustration`(决策+校验) → `drawio-chart`(画图) |
| ⑧ 审校-逐段 | "逐段 review""标 VERSION 增量评审""快速过一遍" | `tech-article-review` |
| ⑧ 审校-多维度并行 | "全面审稿""发布前检查""多角度并行审查+批量改" | `multi-review-pipeline` |
| ⑧ 审校-聚合报告 | "4 维度并行审校出报告（事实/一致性/风格/结构）" | `review-doc` |
| ⑨ 查重/洗稿 | "这篇是不是抄了来源""查洗稿""只可参考不可抄袭" | `plagiarism-audit`（需提供外部来源） |
| ⑩ 发布前总检 | "文章写完了，发布前过一遍" | 见下方 §3 默认 pipeline |

## 2. 易混 skill 区分

- **起草三选**：从零创作 → `article-writer`；已有碎片要拼 → `writing-shape`；要边写边选支线 → `writing-beats`。
- **改写四选**：调结构/逻辑 → `edit-article`；去 AI 味/文风 → `ai-text-polisher`；纯排版/标点/空格 → `chinese-markdown-normalizer`；**深度改写整篇**（骨架+措辞都要动）→ `article-writer`（深度改写模式）。前三者可串行：先 edit-article → 再 polisher → 最后 normalizer。
- **审校三选**：逐段增量、单次快评 → `tech-article-review`；多维度并行 + **直接批量改** → `multi-review-pipeline`；多维度并行只要**报告**（事实核查/一致性/风格/结构）→ `review-doc`。**裁决：要改选 multi-review-pipeline，要报告选 review-doc，二选一不叠加。**
- **调研三选**：仓库内 .md 查找 → `doc-finder`；全网多源带引用 → `deep-research`；产品/公司/技术深度研究 PDF → `hv-analysis`。
- **配图两件套**：先 `drawio-article-illustration` 判断"哪里需要图、用什么图"，再 `drawio-chart` 实际画。不要跳过前者直接画。

## 3. 默认 pipeline（端到端）

> 🔴 **CHECKPOINT**：跨多阶段 pipeline 前，先与用户确认范围（跑全链路 / 只跑某几步 / 单点）。

### 通用技术写作
`doc-finder/deep-research`(选型) → `article-writer`(起草) → `edit-article`(结构) → `ai-text-polisher`(去AI味) → `drawio-article-illustration`+`drawio-chart`(配图) → `multi-review-pipeline`(审校改) → `plagiarism-audit`(查重，**需外部来源，否则跳过**) → `chinese-markdown-normalizer`(排版)

> 详细串联顺序与边界 case 见 [REFERENCE.md](REFERENCE.md) §6。

### JavaGuide 模式
同上，但第②步 `article-writer` **强制 JavaGuide 模式**（遵循项目 docs/ai/ 系列规范），审校阶段优先 `tech-article-review`（贴合 JavaGuide 风格）。

## 4. 路由执行规则

1. 读用户请求 → 匹配 §1 表中"用户在说什么"列。
2. 命中单一阶段 → 直接调用对应 skill，**不重复本 skill**。
3. 跨阶段（如"写并审"）→ 🔴 **CHECKPOINT**：先确认跑全链路还是单点，再按默认 pipeline 顺序调用多个 skill，逐个执行。
4. 模糊/无法匹配 → 🛑 **STOP**：读 [REFERENCE.md](REFERENCE.md) 取详细决策规则；仍无法定 → 反问用户三要素（文体/阶段/目标仓库），**不擅自猜测路由**。
5. **不要**自己重写各 skill 已封装的能力（如手工去 AI 味、手工查重）——一律委派。

## 5. JavaGuide 模式开关

用户提以下任一关键词 → 全 pipeline 切 JavaGuide 模式：JavaGuide、docs/ai、Snailclimb、Java 面试系列、`my-submission/`。
切模式后：起草用 `article-writer` JavaGuide 模式；审校首选 `tech-article-review`；查重对齐"只可参考不可抄袭"铁律（见 [REFERENCE.md](REFERENCE.md) §3）。

详细路由规则、边界 case、多 skill 串联顺序见 [REFERENCE.md](REFERENCE.md)。
