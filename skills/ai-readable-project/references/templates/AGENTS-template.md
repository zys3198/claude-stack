# <项目名> — AI 项目上下文

> 本文件为项目 AI 上下文**源文件**（Copilot/其他 agent 读取）。CLAUDE.md 通过 `@AGENTS.md` 导入本文件，使 Claude Code 读到同一份内容。

## 项目简介
<!-- 1~2 句：项目做什么、面向谁、核心价值 -->

## 技术栈
<!-- 语言 / 框架 / 关键依赖 / 构建工具 -->

## 知识索引
此处描述各个领域的知识需要去哪里找：
<!-- 知识分类骨架（按需取舍，勿为分类而分类）：specs=要做什么（业务地图） changelog=做了/将做什么 architecture=怎么连接 runbook=过程知识/踩坑/约定 vendor=外部源码选择性同步（只同步实际需要的） wiki=人工自由维护 -->
- 业务背景知识 → <!-- 路径 -->
- 架构信息&技术方案沉淀 → <!-- 路径 -->
- 通用组件&规范 → <!-- 路径 -->
- 三方的对接系统信息 → <!-- 路径 -->
- 其他业务规范 → <!-- 路径 -->

## 项目规范要求
<!-- 仅记录项目已有的规范（commit message 格式、代码风格、目录约定、禁止事项等），不在此新制定规范。草稿阶段留空待负责人补充。 -->
- <!-- 项目特有：commit message 格式、代码风格、目录约定、禁止事项等 -->

### 知识落盘规范
- 根目录只保留索引概要（路径 + 1~2 句摘要），不在此堆细节。
- 当对话中出现可复用的规则/兼容性/排障结论/项目知识时，必须就近落盘到对应模块说明，并同步更新根索引（以模块内容为准）。
- 当发现知识索引出现内容过期或不准确时，主动修改。

### 边界与验证
- Identity：本项目的 agent 是谁、知识从哪里读。
- Boundary：能碰什么、不能碰什么（默认拒绝）。
- Validation：改完必须通过哪些检查（lint/类型/测试）。

<!-- 行为契约骨架（按需取舍，写给本项目的 agent）：GOAL=最终可验收结果与交付标准；AUTONOMY=常规缺口自行假设并继续，只有缺失信息会实质改变结果才提问；PRIORITY=用户当场显式指令 > 本文件 > skills 一般指导，被规则卡住时报文件+规则原文；DONE=做到完成并给出可用性验证，不是只给计划；APPROVAL=可逆操作直接做完交付可审阅结果，批准是最后一步，不可逆操作先确认；VERIFY=验证与改动成比例，必要检查通过即停；SUBAGENTS=独立子任务可并行就并行，主代理整合裁决 -->

## 模块说明
<!-- 指向 modules/<module>-AGENTS.md 或模块内文件，每项 1~2 句 + 路径。落地时各模块说明就近放置到对应模块目录。 -->
